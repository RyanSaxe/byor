id: python.no-eval
valid:
  # Parsing data is the sanctioned alternative.
  - value = ast.literal_eval(raw)
  # A method named eval on another object is not the builtin.
  - model.eval()
invalid:
  - result = eval(expression)
  - result = eval(expression, globals(), locals())
  - result = builtins.eval(expression)
