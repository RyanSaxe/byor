id: python.no-exec
valid:
  # A method named exec on another object is not the builtin.
  - cursor.exec(query)
  # os.execv is process replacement, not dynamic code execution.
  - os.execv(path, args)
invalid:
  - exec(code)
  - exec(code, namespace)
  - builtins.exec(code)
