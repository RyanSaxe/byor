"""Group filesystem and process IO helpers.

The IO package contains small adapters for atomic writes, git commands, paths, YAML, and intentional
stdout output. Keeping these helpers isolated makes side effects easier to audit under the dogfood
rules.
"""

__all__ = ()
